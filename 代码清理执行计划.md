# 代码清理执行计划

**创建时间**: 2026-02-21  
**优先级**: 🔴 P0 - 立即执行  
**预计时间**: 2-3小时

---

## 🎯 核心发现

通过分析实际使用情况，发现了关键架构信息：

### 实际使用的组件来源

| 角色 | 检测器来源 | 分析器来源 | 决策器来源 |
|------|-----------|-----------|-----------|
| **BaseGoodAgent** | `villager.detectors` | `villager.analyzers` | `villager.decision_makers` |
| **Hunter** | 继承自BaseGoodAgent | `hunter.analyzers`（特有） | `hunter.decision_makers`（特有）|
| **Guard** | 继承自BaseGoodAgent | 继承自BaseGoodAgent | `guard.decision_makers`（特有）|
| **Seer** | 继承自BaseGoodAgent | `seer.analyzers`（特有） | `seer.decision_makers`（特有）|
| **Villager** | `villager.detectors` | `villager.analyzers` | `villager.decision_makers` |
| **Witch** | 继承自BaseGoodAgent | 继承自BaseGoodAgent | 继承自BaseGoodAgent |
| **Wolf** | 继承自BaseWolfAgent | 继承自BaseWolfAgent | 继承自BaseWolfAgent |
| **WolfKing** | 继承自BaseWolfAgent | 继承自BaseWolfAgent | 继承自BaseWolfAgent |

### 关键结论

1. **BaseGoodAgent使用villager的组件作为默认实现** ✅
2. **Hunter、Seer有自己的特有组件** ✅
3. **Guard只有特有的决策器，其他继承** ✅
4. **Common模块的组件基本未被使用** ⚠️

---

## 🔍 死代码识别

### 1. Common模块 - 大部分未使用 🔴

#### 未使用的分析器
```python
# werewolf/common/analyzers.py
class TrustAnalyzer(BaseTrustManager):  # ❌ 未使用
class VotingAnalyzer(BaseAnalyzer):     # ❌ 未使用
class WolfProbabilityAnalyzer(BaseAnalyzer):  # ❌ 未使用
```

**原因**: BaseGoodAgent直接使用villager的实现

#### 未使用的决策器
```python
# werewolf/common/decision_makers.py
class VoteDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
class SheriffDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
```

**原因**: BaseGoodAgent直接使用villager的实现

#### 未使用的检测器
```python
# werewolf/common/detectors.py
class InjectionDetector(BaseDetector):  # ❌ 未使用
class FalseQuoteDetector(BaseDetector):  # ❌ 未使用
class SpeechQualityDetector(BaseDetector):  # ❌ 未使用
```

**原因**: BaseGoodAgent直接使用villager的实现

**建议**: 
- ❌ 删除common模块中未使用的组件
- ✅ 保留BaseDetector、BaseAnalyzer、BaseDecisionMaker等基类

---

### 2. Hunter模块 - 部分重复 🟡

#### 重复的分析器（与villager重复）
```python
# werewolf/hunter/analyzers.py
class VotingPatternAnalyzer(BaseAnalyzer):  # ⚠️ 与villager重复
```

**实际使用**: Hunter导入了自己的VotingPatternAnalyzer

**建议**: 
- ✅ 保留hunter的VotingPatternAnalyzer（实际在用）
- ❌ 删除villager中的VotingPatternAnalyzer（hunter不用）
- 或者：让hunter继承villager的实现

#### 特有的分析器（保留）
```python
# werewolf/hunter/analyzers.py
class TrustScoreAnalyzer(BaseAnalyzer):  # ✅ 特有，保留
class SpeechQualityAnalyzer(BaseAnalyzer):  # ✅ 特有，保留
class ThreatLevelAnalyzer(BaseAnalyzer):  # ✅ 特有，保留
class WolfProbabilityCalculator:  # ✅ 特有，保留
class MemoryDAO:  # ✅ 特有，保留
```

#### 特有的决策器（保留）
```python
# werewolf/hunter/decision_makers.py
class ShootDecisionMaker(BaseDecisionMaker):  # ✅ 特有，保留
```

#### 重复的决策器（未使用）
```python
# werewolf/hunter/decision_makers.py
class VoteDecisionMaker(BaseDecisionMaker):  # ❌ 未使用，继承自BaseGoodAgent
class SheriffElectionDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
class SheriffVoteDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
```

**建议**: 删除hunter中未使用的决策器

---

### 3. Seer模块 - 部分重复 🟡

#### 特有的分析器（保留）
```python
# werewolf/seer/analyzers.py
class CheckPriorityCalculator:  # ✅ 特有，保留
```

#### 重复的分析器（未使用）
```python
# werewolf/seer/analyzers.py
class VotingPatternAnalyzer(BaseAnalyzer):  # ❌ 未使用，继承自BaseGoodAgent
class GamePhaseAnalyzer(BaseAnalyzer):  # ❌ 未使用，继承自BaseGoodAgent
```

**建议**: 删除seer中未使用的分析器

#### 特有的决策器（保留）
```python
# werewolf/seer/decision_makers.py
class CheckDecisionMaker(BaseDecisionMaker):  # ✅ 特有，保留
class IdentityRevealDecisionMaker(BaseDecisionMaker):  # ✅ 特有，保留
```

#### 重复的决策器（未使用）
```python
# werewolf/seer/decision_makers.py
class VoteDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
class SheriffElectionDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
class SheriffVoteDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
class BadgeTransferDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
class SpeechOrderDecisionMaker(BaseDecisionMaker):  # ❌ 未使用
```

**建议**: 删除seer中未使用的决策器

---

### 4. Villager模块 - 核心模块，保留 ✅

Villager模块是BaseGoodAgent的默认实现，所有组件都在使用：

```python
# werewolf/villager/detectors.py
class InjectionDetector(LLMDetectorBase):  # ✅ BaseGoodAgent使用
class FalseQuoteDetector(LLMDetectorBase):  # ✅ BaseGoodAgent使用
class MessageParser:  # ✅ BaseGoodAgent使用
class SpeechQualityEvaluator:  # ✅ BaseGoodAgent使用

# werewolf/villager/analyzers.py
class TrustScoreManager:  # ✅ BaseGoodAgent使用
class TrustScoreCalculator:  # ✅ BaseGoodAgent使用
class VotingPatternAnalyzer(BaseAnalyzer):  # ✅ BaseGoodAgent使用
class GamePhaseAnalyzer(BaseAnalyzer):  # ✅ BaseGoodAgent使用
class SpeechPositionAnalyzer(BaseAnalyzer):  # ✅ VillagerAgent使用

# werewolf/villager/decision_makers.py
class VoteDecisionMaker(BaseDecisionMaker):  # ✅ BaseGoodAgent使用
class SheriffElectionDecisionMaker(BaseDecisionMaker):  # ✅ BaseGoodAgent使用
class SheriffVoteDecisionMaker(BaseDecisionMaker):  # ✅ BaseGoodAgent使用
class BadgeTransferDecisionMaker(BaseDecisionMaker):  # ✅ VillagerAgent使用
class SpeechOrderDecisionMaker(BaseDecisionMaker):  # ✅ VillagerAgent使用
class LastWordsGenerator:  # ✅ VillagerAgent使用
```

**建议**: 保留villager模块的所有组件

---

### 5. Guard模块 - 严重问题 🔴

#### 双重架构问题
```python
# werewolf/guard/decision_makers.py

# 第一套：Strategy模式（30-402行）
class GuardDecisionStrategy(DecisionStrategy):  # ❌ 未使用
class VoteDecisionStrategy(DecisionStrategy):  # ❌ 未使用

# 第二套：企业级模式（404-end）
class GuardDecisionMaker:  # ✅ GuardAgent使用
class VoteDecisionMaker:  # ❌ 未使用，继承自BaseGoodAgent
```

**实际使用**: GuardAgent只使用`GuardDecisionMaker`

**建议**: 
- ✅ 保留`GuardDecisionMaker`
- ❌ 删除`GuardDecisionStrategy`和`VoteDecisionStrategy`
- ❌ 删除`VoteDecisionMaker`（继承自BaseGoodAgent）

#### 重复的分析器（全部未使用）
```python
# werewolf/guard/analyzers.py
class WolfProbabilityAnalyzer:  # ❌ 未使用，继承自BaseGoodAgent
class VotingPatternAnalyzer:  # ❌ 未使用，继承自BaseGoodAgent
```

**建议**: 删除guard/analyzers.py整个文件

#### 重复的检测器（全部未使用）
```python
# werewolf/guard/detectors.py
class InjectionDetector:  # ❌ 未使用，继承自BaseGoodAgent
class FalseQuotationDetector:  # ❌ 未使用，继承自BaseGoodAgent
class StatusContradictionDetector:  # ❌ 未使用
class SpeechQualityDetector:  # ❌ 未使用，继承自BaseGoodAgent
```

**建议**: 删除guard/detectors.py整个文件

---

### 6. Wolf模块 - 最小重复 🟢

#### 重复的检测器（未使用）
```python
# werewolf/wolf/detectors.py
class InjectionDetector:  # ❌ 未使用
```

**建议**: 删除wolf/detectors.py整个文件

---

## 📋 清理清单

### 阶段1: 删除死代码（P0 - 立即执行）

#### 1.1 删除整个文件
```bash
# 删除未使用的文件
rm werewolf/guard/analyzers.py
rm werewolf/guard/detectors.py
rm werewolf/wolf/detectors.py
```

#### 1.2 删除common模块中的死代码
```python
# werewolf/common/analyzers.py
# 删除以下类（保留基类）
- class TrustAnalyzer(BaseTrustManager)  # 13-159行
- class VotingAnalyzer(BaseAnalyzer)  # 161-278行
- class WolfProbabilityAnalyzer(BaseAnalyzer)  # 280-end

# werewolf/common/decision_makers.py
# 删除以下类（保留基类）
- class VoteDecisionMaker(BaseDecisionMaker)  # 14-193行
- class SheriffDecisionMaker(BaseDecisionMaker)  # 195-end

# werewolf/common/detectors.py
# 删除以下类（保留基类）
- class InjectionDetector(BaseDetector)  # 14-106行
- class FalseQuoteDetector(BaseDetector)  # 108-251行
- class SpeechQualityDetector(BaseDetector)  # 253-end
```

#### 1.3 删除hunter模块中的死代码
```python
# werewolf/hunter/decision_makers.py
# 删除以下类
- class VoteDecisionMaker(BaseDecisionMaker)  # 37-258行
- class SheriffElectionDecisionMaker(BaseDecisionMaker)  # 495-550行
- class SheriffVoteDecisionMaker(BaseDecisionMaker)  # 552-end
```

#### 1.4 删除seer模块中的死代码
```python
# werewolf/seer/analyzers.py
# 删除以下类
- class VotingPatternAnalyzer(BaseAnalyzer)  # 353-412行
- class GamePhaseAnalyzer(BaseAnalyzer)  # 413-end

# werewolf/seer/decision_makers.py
# 删除以下类
- class VoteDecisionMaker(BaseDecisionMaker)  # 17-174行
- class SheriffElectionDecisionMaker(BaseDecisionMaker)  # 203-265行
- class SheriffVoteDecisionMaker(BaseDecisionMaker)  # 267-336行
- class BadgeTransferDecisionMaker(BaseDecisionMaker)  # 338-400行
- class SpeechOrderDecisionMaker(BaseDecisionMaker)  # 402-462行
```

#### 1.5 删除guard模块中的死代码
```python
# werewolf/guard/decision_makers.py
# 删除以下类
- class GuardDecisionStrategy(DecisionStrategy)  # 30-155行
- class VoteDecisionStrategy(DecisionStrategy)  # 157-402行
- class VoteDecisionMaker  # 519-end
```

---

### 阶段2: 验证测试（P0 - 立即执行）

```bash
# 运行所有测试
python run_tests.py

# 运行ML增强测试
python test_ml_enhanced.py

# 检查导入错误
python -c "from werewolf.hunter.hunter_agent import HunterAgent; print('✓ Hunter OK')"
python -c "from werewolf.guard.guard_agent import GuardAgent; print('✓ Guard OK')"
python -c "from werewolf.seer.seer_agent import SeerAgent; print('✓ Seer OK')"
python -c "from werewolf.villager.villager_agent import VillagerAgent; print('✓ Villager OK')"
python -c "from werewolf.witch.witch_agent import WitchAgent; print('✓ Witch OK')"
python -c "from werewolf.wolf.wolf_agent import WolfAgent; print('✓ Wolf OK')"
python -c "from werewolf.wolf_king.wolf_king_agent import WolfKingAgent; print('✓ WolfKing OK')"
```

---

### 阶段3: 清理导入语句（P1 - 重要）

检查并删除未使用的导入：

```bash
# 检查未使用的导入
grep -r "from werewolf.guard.analyzers import" werewolf/
grep -r "from werewolf.guard.detectors import" werewolf/
grep -r "from werewolf.wolf.detectors import" werewolf/
grep -r "from werewolf.common.analyzers import" werewolf/
grep -r "from werewolf.common.decision_makers import" werewolf/
grep -r "from werewolf.common.detectors import" werewolf/
```

---

## 📊 预期收益

### 代码量减少

| 模块 | 删除行数 | 保留行数 | 减少比例 |
|------|---------|---------|---------|
| common/analyzers.py | ~450行 | ~50行 | 90% |
| common/decision_makers.py | ~380行 | ~50行 | 88% |
| common/detectors.py | ~450行 | ~50行 | 89% |
| guard/analyzers.py | ~200行 | 0行 | 100% |
| guard/detectors.py | ~350行 | 0行 | 100% |
| guard/decision_makers.py | ~400行 | ~120行 | 77% |
| hunter/decision_makers.py | ~520行 | ~260行 | 50% |
| seer/analyzers.py | ~250行 | ~350行 | 0% (保留特有) |
| seer/decision_makers.py | ~400行 | ~200行 | 50% |
| wolf/detectors.py | ~100行 | 0行 | 100% |
| **总计** | **~3500行** | **~1080行** | **69%** |

### 文件减少

- 删除文件: 3个
- 大幅精简文件: 7个
- 总文件数减少: ~30%

---

## ⚠️ 风险评估

### 低风险 🟢
- 删除guard/analyzers.py
- 删除guard/detectors.py
- 删除wolf/detectors.py
- 删除common模块中的死代码

**原因**: 这些代码完全未被使用

### 中风险 🟡
- 删除hunter中的重复决策器
- 删除seer中的重复决策器和分析器

**原因**: 需要确认继承链正确

### 缓解措施
1. 先运行完整测试套件
2. 逐个模块清理
3. 每次清理后立即测试
4. 保留git历史，方便回滚

---

## ✅ 执行步骤

### Step 1: 备份（5分钟）
```bash
git add .
git commit -m "Backup before code cleanup"
git branch backup-before-cleanup
```

### Step 2: 删除整个文件（5分钟）
```bash
rm werewolf/guard/analyzers.py
rm werewolf/guard/detectors.py
rm werewolf/wolf/detectors.py
```

### Step 3: 清理common模块（20分钟）
- 编辑werewolf/common/analyzers.py
- 编辑werewolf/common/decision_makers.py
- 编辑werewolf/common/detectors.py

### Step 4: 清理hunter模块（15分钟）
- 编辑werewolf/hunter/decision_makers.py

### Step 5: 清理seer模块（20分钟）
- 编辑werewolf/seer/analyzers.py
- 编辑werewolf/seer/decision_makers.py

### Step 6: 清理guard模块（15分钟）
- 编辑werewolf/guard/decision_makers.py

### Step 7: 测试验证（30分钟）
```bash
python run_tests.py
python test_ml_enhanced.py
# 测试每个角色代理人
```

### Step 8: 清理导入（10分钟）
- 检查并删除未使用的导入语句

### Step 9: 最终测试（20分钟）
```bash
python run_tests.py
# 完整的集成测试
```

### Step 10: 提交（5分钟）
```bash
git add .
git commit -m "Clean up dead code and duplicates - removed 3500 lines"
```

---

## 🎯 总结

### 清理目标
- ✅ 删除3500行死代码
- ✅ 删除3个完全未使用的文件
- ✅ 精简7个文件
- ✅ 保持100%测试通过率
- ✅ 保持所有功能正常

### 预计时间
- 总时间: 2-3小时
- 核心清理: 1.5小时
- 测试验证: 1小时
- 文档更新: 0.5小时

### 立即开始
**建议立即开始执行Step 1-2，删除最明显的死代码！**

---

**计划完成** ✅  
**准备执行** 🚀

---

**文档版本**: 1.0.0  
**创建时间**: 2026-02-21  
**状态**: 🔴 待执行
